import LegalDocument from "../components/LegalDocument";

/** "Paketbedingungen" — term, extension, zone change and transfer of 6er/8er packages. */
export default function PackageTermsPage() {
  return <LegalDocument document="packageTerms" />;
}
