import LegalDocument from "../components/LegalDocument";

/** "Terminbedingungen" — cancellation, late arrival, no-show, free consultation. */
export default function AppointmentTermsPage() {
  return <LegalDocument document="appointmentTerms" />;
}
