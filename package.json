{
  "name": "kosmetic-fuessen",
  "version": "0.1.0",
  "private": true,
  "description": "Landing page + booking API for a cosmetic procedures cabinet in Füssen",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently -n client,server -c cyan,magenta \"npm run dev -w client\" \"npm run dev -w server\"",
    "build": "npm run build -w client && npm run build -w server",
    "test": "npm run test -w client && npm run test -w server",
    "test:e2e": "playwright test",
    "typecheck": "npm run typecheck -w client && npm run typecheck -w server",
    "db:migrate": "npm run db:migrate -w server"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "concurrently": "^9.1.0"
  },
  "engines": {
    "node": ">=20"
  }
}
